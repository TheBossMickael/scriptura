# Applies to the repo-root build context (the `front` service). Keep the context small and
# force fresh installs inside the image.
**/node_modules
**/.env
.git
contracts/out
contracts/cache
contracts/broadcast
relayer/dist
indexer/.ponder
frontend/dist
